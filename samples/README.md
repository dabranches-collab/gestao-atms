# Ficheiros de teste locais

Coloque aqui, apenas localmente, os ficheiros `.xlsx` usados para validação. A pasta está protegida pelo `.gitignore`; nenhum relatório reservado deve ser enviado para Git.
